const restRouter = require('./restaurants');
const scoutsRouter = require('./scouts');
const clientRouter = require('./clients');
const orderRouter = require('./orders');

module.exports = {
  restRouter,
  scoutsRouter,
  clientRouter,
  orderRouter,
};